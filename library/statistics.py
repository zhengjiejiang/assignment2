# Task3

#task3
def value():
    max_value = max()

    min_value = min()


#task4 and task5

import statistics

def caculate_formula(value):

    mean = statistics.mean(value)

    median_value = statistics.median(value)

    mode = statistics.mode(value)

    range = len(value)

    standard_deviation = statistics.stdev(value)

    variance = statistics.variance(svalue)
